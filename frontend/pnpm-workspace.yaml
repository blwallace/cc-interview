# pnpm 11 blocks dependency postinstall scripts unless listed here.
# esbuild needs its postinstall to link the platform binary; @scarf/scarf is telemetry, so no.
allowBuilds:
  '@scarf/scarf': false
  esbuild: true
